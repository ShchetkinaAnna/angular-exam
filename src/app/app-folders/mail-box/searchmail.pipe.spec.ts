import { SearchmailPipe } from './searchmail.pipe';

describe('SearchmailPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchmailPipe();
    expect(pipe).toBeTruthy();
  });
});
