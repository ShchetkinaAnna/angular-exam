import { ValidateBdDirective } from './validate-bd.directive';

describe('ValidateBdDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateBdDirective();
    expect(directive).toBeTruthy();
  });
});
